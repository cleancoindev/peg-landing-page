{
  "name": "peg-landing-page",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "7.17.9",
    "meteor-node-stubs": "^0.4.1"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
